# Kafka topic configuration generated by Backstage
topicName: ${{ values.topic_name | lower | replace(" ", "-") }}
partitions: ${{ values.partitions }}
replicationFactor: ${{ values.replication_factor }}
owner: ${{ values.owner }}
description: "Kafka topic ${{ values.topic_name }} managed by Backstage"
